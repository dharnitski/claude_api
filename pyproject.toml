[project]
name = "claude-api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "anthropic",
    "python-dotenv",
    "mcp[cli]<2",
    "prompt-toolkit",
    "pydantic",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
    "mypy",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]
ignore = ["E501"]

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.mypy]
strict = true
